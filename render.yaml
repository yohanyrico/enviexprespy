services:
  - type: web
    name: enviexpress
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8080
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SECRET_KEY
        sync: false